{
  "name": "costmap-api",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/costmap/api/src",
  "targets": {
    "dev": {
      "command": "./gradlew quarkusDev",
      "options": {
        "cwd": "apps/costmap/api"
      }
    },
    "build": {
      "command": "./gradlew build",
      "options": {
        "cwd": "apps/costmap/api"
      }
    }
  }
}
